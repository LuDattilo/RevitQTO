<Window x:Class="QtoRevitPlugin.UI.Views.SessionListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:QtoRevitPlugin.UI.ViewModels"
        Title="Riprendi Sessione"
        Height="480" Width="720"
        WindowStartupLocation="CenterOwner"
        Background="#F0F0F0"
        ResizeMode="CanResize">

    <Window.DataContext>
        <vm:SessionListViewModel />
    </Window.DataContext>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Sessioni per il progetto corrente"
                       FontSize="14" FontWeight="SemiBold" />
            <TextBlock Text="{Binding ProjectName}"
                       FontSize="11" Foreground="#666" Margin="0,2,0,0" />
        </StackPanel>

        <!-- Lista sessioni -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Sessions}"
                  SelectedItem="{Binding SelectedSession}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  RowHeight="34"
                  AlternatingRowBackground="#F8F8F8"
                  BorderBrush="#CCCCCC">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Sessione" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Stato" Binding="{Binding Status}" Width="90" />
                <DataGridTextColumn Header="Avanzamento" Binding="{Binding Progress}" Width="130" />
                <DataGridTextColumn Header="Importo" Binding="{Binding Amount}" Width="110" />
                <DataGridTextColumn Header="Ultima voce" Binding="{Binding LastEpCode}" Width="100" />
                <DataGridTextColumn Header="Salvata il" Binding="{Binding LastSaved}" Width="120" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Messaggio "nessuna sessione" -->
        <TextBlock Grid.Row="1"
                   Text="Nessuna sessione precedente per questo progetto."
                   FontSize="12" Foreground="#999"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Visibility="{Binding HasSessions, Converter={x:Static vm:InverseBoolToVisibility.Instance}}" />

        <!-- Bottoni -->
        <StackPanel Grid.Row="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Nuova Sessione"
                    Name="NewSessionButton"
                    Padding="14,6" Margin="0,0,8,0"
                    MinWidth="130" />
            <Button Content="Riprendi"
                    Name="ResumeButton"
                    Padding="14,6" Margin="0,0,8,0"
                    MinWidth="110"
                    IsDefault="True"
                    Background="#1F4E79" Foreground="White" BorderThickness="0"
                    IsEnabled="{Binding SelectedSession, Converter={x:Static vm:NotNullToBool.Instance}}" />
            <Button Content="Annulla"
                    Name="CancelButton"
                    Padding="14,6"
                    MinWidth="90"
                    IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
