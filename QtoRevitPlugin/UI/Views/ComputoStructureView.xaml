<UserControl x:Class="QtoRevitPlugin.UI.Views.ComputoStructureView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:QtoRevitPlugin.UI.ViewModels"
             xmlns:dd="urn:gong-wpf-dragdrop">
    <UserControl.DataContext>
        <vm:ComputoStructureViewModel/>
    </UserControl.DataContext>

    <Grid Margin="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar CRUD -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,6">
            <Button Content="+ Super" Command="{Binding AddSuperCommand}" Style="{DynamicResource BtnTool}" Margin="0,0,4,0"/>
            <Button Content="+ Cat" Command="{Binding AddCategoryCommand}" Style="{DynamicResource BtnTool}" Margin="0,0,4,0"/>
            <Button Content="+ Sub" Command="{Binding AddSubCategoryCommand}" Style="{DynamicResource BtnTool}" Margin="0,0,4,0"/>
            <Button Content="Rinomina" Command="{Binding RenameCommand}" Style="{DynamicResource BtnTool}" Margin="0,0,4,0"/>
            <Button Content="Elimina" Command="{Binding DeleteCommand}" Style="{DynamicResource BtnTool}" Margin="0,0,8,0"/>
            <Separator Width="1" Background="{DynamicResource EdgeMediumBrush}" Margin="0,2,8,2"/>
            <Button Content="↑" Command="{Binding MoveUpCommand}" Style="{DynamicResource BtnTool}" Margin="0,0,2,0" ToolTip="Sposta su"/>
            <Button Content="↓" Command="{Binding MoveDownCommand}" Style="{DynamicResource BtnTool}" Margin="0,0,0,0" ToolTip="Sposta giù"/>
        </StackPanel>

        <!-- TreeView con drag&drop (GongSolutions) -->
        <TreeView x:Name="ChaptersTree" Grid.Row="1" ItemsSource="{Binding Roots}"
                  SelectedItemChanged="OnTreeSelectedItemChanged"
                  dd:DragDrop.IsDragSource="True"
                  dd:DragDrop.IsDropTarget="True"
                  dd:DragDrop.DropHandler="{Binding}">
            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem">
                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                    <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
                </Style>
            </TreeView.ItemContainerStyle>
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                    <TextBlock Text="{Binding DisplayLabel}"/>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>

        <!-- Pannello dettaglio nodo selezionato: ComboBox OG/OS -->
        <Border Grid.Row="2" Margin="0,6,0,0"
                Background="{DynamicResource PanelSubBrush}"
                BorderBrush="{DynamicResource EdgeLightBrush}" BorderThickness="1"
                Padding="10,8">
            <StackPanel>
                <TextBlock Text="CODICE SOA (OG / OS) — D.LGS. 36/2023 ALL. II.12"
                           Style="{DynamicResource SectionLabelStyle}" />
                <Grid Margin="0,6,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Nodo selezionato:" VerticalAlignment="Center" Margin="0,0,8,0"
                               FontSize="11" Foreground="{DynamicResource InkMutedBrush}" />
                    <ComboBox Grid.Column="1"
                              ItemsSource="{Binding AvailableSoa}"
                              SelectedItem="{Binding SelectedNodeSoa, Mode=TwoWay}"
                              IsEnabled="{Binding SelectedNode, Converter={x:Static vm:NullToBoolConverter.Instance}}"
                              FontSize="11">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Code}" FontFamily="{DynamicResource FontMono}"
                                               FontWeight="SemiBold" Width="60" />
                                    <TextBlock Text="{Binding Description}" Foreground="{DynamicResource InkMutedBrush}"
                                               TextTrimming="CharacterEllipsis" />
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </Grid>
                <TextBlock FontSize="10" FontStyle="Italic" Margin="0,4,0,0"
                           Foreground="{DynamicResource InkMutedBrush}"
                           Text="Il codice assegnato a un nodo si propaga ai figli per eredità. Rimuovi selezione per cancellare l'assegnazione propria (il nodo erediterà dal parent se presente)." />
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Foreground="{DynamicResource InkMutedBrush}" Margin="0,4,0,0"/>
    </Grid>
</UserControl>
