<Window x:Class="QtoRevitPlugin.UI.Views.ChapterEditorPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Modifica capitolo"
        Width="380" Height="180"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Codice:" Margin="0,0,0,2"/>
        <TextBox Grid.Row="1" Name="CodeBox" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2" Text="Nome:" Margin="0,0,0,2"/>
        <TextBox Grid.Row="3" Name="NameBox" VerticalAlignment="Top"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Width="80" Margin="0,0,8,0" Click="OnOkClick" IsDefault="True"/>
            <Button Content="Annulla" Width="80" Click="OnCancelClick" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
