# Sprint 6–7–8 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Aggiungere layer di astrazione repository + audit trail + Preferiti + MappingRules + Model Diff Check al plugin CME Revit.

**Architecture:** Sprint 6 introduce interfacce repository (IQtoRepository, IPriceListRepository, IFavoritesRepository) e audit fields su QtoAssignment (CreatedBy, Version, Status) senza cambiare il flusso utente. Sprint 7 aggiunge UI Prezzario separata con Preferiti e MappingRules JSON per parametro quantitativo per-categoria. Sprint 8 implementa Model Diff Check con snapshot SHA256 per elemento e dialog di riconciliazione WPF.

**Tech Stack:** C# / WPF / Revit API 2022–2027, net48 (R2022–2024) + net8.0-windows (R2025–2027), SQLite + Dapper, CommunityToolkit.Mvvm, System.Text.Json, RevitTask.RunAsync, xUnit (test su net8.0)

---

## File Map

| File | Azione |
|---|---|
| `QtoRevitPlugin.Core/Data/IQtoRepository.cs` | Crea — interfacce IQtoRepository, IFavoritesRepository |
| `QtoRevitPlugin.Core/Data/IPriceListRepository.cs` | Crea — interfaccia IPriceListRepository |
| `QtoRevitPlugin.Core/Services/IUserContext.cs` | Crea — IUserContext + WindowsUserContext |
| `QtoRevitPlugin.Core/Models/ChangeLogEntry.cs` | Crea — modello ChangeLogEntry |
| `QtoRevitPlugin.Core/Models/FavoriteSet.cs` | Crea — modello FavoriteSet + FavoriteItem |
| `QtoRevitPlugin.Core/Models/MappingRule.cs` | Crea — modello MappingRule + MappingRulesConfig |
| `QtoRevitPlugin.Core/Models/ElementSnapshot.cs` | Crea — modello ElementSnapshot |
| `QtoRevitPlugin.Core/Models/AssignmentStatus.cs` | Crea — enum AssignmentStatus |
| `QtoRevitPlugin.Core/Models/QtoAssignment.cs` | Modifica — aggiunge audit fields |
| `QtoRevitPlugin.Core/Data/DatabaseSchema.cs` | Modifica — v4: ChangeLog table + audit columns + ElementSnapshots table |
| `QtoRevitPlugin.Core/Data/DatabaseInitializer.cs` | Modifica — migrazione v3→v4 |
| `QtoRevitPlugin.Core/Data/QtoRepository.cs` | Modifica — implementa IQtoRepository + metodi ChangeLog |
| `QtoRevitPlugin.Core/Data/FileFavoritesRepository.cs` | Crea — IFavoritesRepository su filesystem JSON |
| `QtoRevitPlugin.Core/Services/MappingRulesService.cs` | Crea — carica/salva QTO_MappingRules.json |
| `QtoRevitPlugin/Services/ModelDiffService.cs` | Crea — ComputeDiff + calcolo SHA256 |
| `QtoRevitPlugin/Application/QtoApplication.cs` | Modifica — singleton CatalogBrowserWindow + IUserContext + ribbon button |
| `QtoRevitPlugin/UI/CatalogBrowserWindow.xaml` | Modifica — aggiunge sezione Preferiti + dropdown MappingRule + pulsante ASSEGNA |
| `QtoRevitPlugin/UI/CatalogBrowserWindow.xaml.cs` | Modifica — logica toggle Show/Hide + selezione Revit |
| `QtoRevitPlugin/UI/ViewModels/CatalogBrowserViewModel.cs` | Modifica — Favorites ObservableCollection + MappingRule + ActiveItem + ASSEGNA command |
| `QtoRevitPlugin/UI/Views/ReconciliationWindow.xaml` | Crea — dialog Model Diff Check |
| `QtoRevitPlugin/UI/Views/ReconciliationWindow.xaml.cs` | Crea — logica riconciliazione + UndoStack |
| `QtoRevitPlugin/UI/ViewModels/ReconciliationViewModel.cs` | Crea — ViewModel per ReconciliationWindow |
| `QtoRevitPlugin.Tests/Sprint6/RepositoryInterfacesTests.cs` | Crea — test interfacce repository |
| `QtoRevitPlugin.Tests/Sprint6/AuditFieldsMigrationTests.cs` | Crea — test migrazione v3→v4 |
| `QtoRevitPlugin.Tests/Sprint7/FavoritesRepositoryTests.cs` | Crea — test FileFavoritesRepository |
| `QtoRevitPlugin.Tests/Sprint7/MappingRulesServiceTests.cs` | Crea — test MappingRulesService |
| `QtoRevitPlugin.Tests/Sprint8/ModelDiffServiceTests.cs` | Crea — test ModelDiffService (con Revit mock) |

---

## Sprint 6 — Fondamenta Dati

### Task 1: AssignmentStatus enum e IUserContext

**Files:**
- Create: `QtoRevitPlugin.Core/Models/AssignmentStatus.cs`
- Create: `QtoRevitPlugin.Core/Services/IUserContext.cs`

- [ ] **Step 1: Crea `AssignmentStatus.cs`**

```csharp
namespace QtoRevitPlugin.Models
{
    public enum AssignmentStatus
    {
        Active,
        Deleted,
        Superseded
    }
}
```

- [ ] **Step 2: Crea `IUserContext.cs`**

```csharp
using System;

namespace QtoRevitPlugin.Services
{
    public interface IUserContext
    {
        string UserId { get; }
    }

    public class WindowsUserContext : IUserContext
    {
        public string UserId => Environment.UserName;
    }
}
```

- [ ] **Step 3: Build Debug R24 per verifica compilazione**

```
msbuild QtoRevitPlugin.sln /p:Configuration="Debug R24" /t:Build /v:m
```

Expected: Build succeeded, 0 Error(s)

- [ ] **Step 4: Commit**

```bash
git add QtoRevitPlugin.Core/Models/AssignmentStatus.cs QtoRevitPlugin.Core/Services/IUserContext.cs
git commit -m "feat(sprint6): AssignmentStatus enum + IUserContext/WindowsUserContext"
```

---

### Task 2: Modelli ChangeLogEntry, FavoriteSet, MappingRule, ElementSnapshot

**Files:**
- Create: `QtoRevitPlugin.Core/Models/ChangeLogEntry.cs`
- Create: `QtoRevitPlugin.Core/Models/FavoriteSet.cs`
- Create: `QtoRevitPlugin.Core/Models/MappingRule.cs`
- Create: `QtoRevitPlugin.Core/Models/ElementSnapshot.cs`

- [ ] **Step 1: Crea `ChangeLogEntry.cs`**

```csharp
using System;

namespace QtoRevitPlugin.Models
{
    public class ChangeLogEntry
    {
        public int ChangeId { get; set; }
        public int SessionId { get; set; }
        public string ElementUniqueId { get; set; } = "";
        public string PriceItemCode { get; set; } = "";
        public string ChangeType { get; set; } = "";   // Created|Updated|Deleted|ModelDiffAccepted
        public string? OldValueJson { get; set; }
        public string? NewValueJson { get; set; }
        public string UserId { get; set; } = "";
        public DateTime Timestamp { get; set; }
    }
}
```

- [ ] **Step 2: Crea `FavoriteSet.cs`**

```csharp
using System;
using System.Collections.Generic;

namespace QtoRevitPlugin.Models
{
    public class FavoriteSet
    {
        public string Name { get; set; } = "";
        public DateTime CreatedAt { get; set; } = DateTime.UtcNow;
        public List<FavoriteItem> Items { get; set; } = new();
    }

    public class FavoriteItem
    {
        public string Code { get; set; } = "";
        public string ShortDesc { get; set; } = "";
        public string Unit { get; set; } = "";
        public double UnitPrice { get; set; }
    }
}
```

- [ ] **Step 3: Crea `MappingRule.cs`**

```csharp
using System.Collections.Generic;

namespace QtoRevitPlugin.Models
{
    public class MappingRulesConfig
    {
        public int Version { get; set; } = 1;
        public List<MappingRule> Rules { get; set; } = new();
    }

    public class MappingRule
    {
        public string RevitCategory { get; set; } = "";
        public string DefaultParam { get; set; } = "Count";
        public List<string> AllowedParams { get; set; } = new();
        public List<string> HashParams { get; set; } = new();
        public string UnitDisplay { get; set; } = "";
        public int RoundingDecimals { get; set; } = 2;
        public bool VuotoPerPieno { get; set; }
    }
}
```

- [ ] **Step 4: Crea `ElementSnapshot.cs`**

```csharp
using System;
using System.Collections.Generic;

namespace QtoRevitPlugin.Models
{
    public class ElementSnapshot
    {
        public int Id { get; set; }
        public int SessionId { get; set; }
        public int ElementId { get; set; }
        public string UniqueId { get; set; } = "";
        public string SnapshotHash { get; set; } = "";  // SHA256 truncated 12 hex chars
        public double SnapshotQty { get; set; }
        public List<string> AssignedEP { get; set; } = new();
        public DateTime LastUpdated { get; set; }
    }
}
```

- [ ] **Step 5: Build Debug R24**

```
msbuild QtoRevitPlugin.sln /p:Configuration="Debug R24" /t:Build /v:m
```

Expected: Build succeeded, 0 Error(s)

- [ ] **Step 6: Commit**

```bash
git add QtoRevitPlugin.Core/Models/ChangeLogEntry.cs QtoRevitPlugin.Core/Models/FavoriteSet.cs QtoRevitPlugin.Core/Models/MappingRule.cs QtoRevitPlugin.Core/Models/ElementSnapshot.cs
git commit -m "feat(sprint6): modelli ChangeLogEntry, FavoriteSet, MappingRule, ElementSnapshot"
```

---

### Task 3: Interfacce Repository

**Files:**
- Create: `QtoRevitPlugin.Core/Data/IQtoRepository.cs`
- Create: `QtoRevitPlugin.Core/Data/IPriceListRepository.cs`

- [ ] **Step 1: Scrivere il test fallente**

Crea `QtoRevitPlugin.Tests/Sprint6/RepositoryInterfacesTests.cs`:

```csharp
using QtoRevitPlugin.Data;
using QtoRevitPlugin.Models;
using System.Collections.Generic;
using Xunit;

namespace QtoRevitPlugin.Tests.Sprint6
{
    public class RepositoryInterfacesTests
    {
        [Fact]
        public void IQtoRepository_HasInsertAssignment()
        {
            // Verifica che l'interfaccia esista e sia implementabile
            var type = typeof(IQtoRepository);
            var method = type.GetMethod("InsertAssignment");
            Assert.NotNull(method);
        }

        [Fact]
        public void IFavoritesRepository_HasLoadGlobal()
        {
            var type = typeof(IFavoritesRepository);
            var method = type.GetMethod("LoadGlobal");
            Assert.NotNull(method);
        }

        [Fact]
        public void IPriceListRepository_HasGetItems()
        {
            var type = typeof(IPriceListRepository);
            var method = type.GetMethod("GetItems");
            Assert.NotNull(method);
        }
    }
}
```

- [ ] **Step 2: Esegui test per verificare che fallisca**

```
dotnet test QtoRevitPlugin.Tests --filter "FullyQualifiedName~RepositoryInterfacesTests" --no-build
```

Expected: FAIL — tipo non trovato

- [ ] **Step 3: Crea `IQtoRepository.cs`**

```csharp
using QtoRevitPlugin.Models;
using System.Collections.Generic;

namespace QtoRevitPlugin.Data
{
    public interface IQtoRepository
    {
        // Sessioni
        int InsertSession(WorkSession session);
        void UpdateSession(WorkSession session);
        WorkSession? GetSession(int sessionId);

        // Assegnazioni
        int InsertAssignment(QtoAssignment assignment);
        void UpdateAssignment(QtoAssignment assignment);
        IReadOnlyList<QtoAssignment> GetAssignments(int sessionId);

        // ChangeLog
        void AppendChangeLog(ChangeLogEntry entry);
        IReadOnlyList<ChangeLogEntry> GetChangeLog(int sessionId);

        // ElementSnapshots
        void UpsertSnapshot(ElementSnapshot snapshot);
        IReadOnlyList<ElementSnapshot> GetSnapshots(int sessionId);
    }

    public interface IFavoritesRepository
    {
        FavoriteSet LoadGlobal();
        void SaveGlobal(FavoriteSet set);
        FavoriteSet? LoadForProject(string cmePath);
        void SaveForProject(string cmePath, FavoriteSet set);
    }
}
```

- [ ] **Step 4: Crea `IPriceListRepository.cs`**

```csharp
using QtoRevitPlugin.Models;
using System.Collections.Generic;

namespace QtoRevitPlugin.Data
{
    public interface IPriceListRepository
    {
        IReadOnlyList<PriceList> GetAllLists();
        IReadOnlyList<PriceItem> GetItems(int listId);
        PriceItem? GetItem(string code);
    }
}
```

- [ ] **Step 5: Build e test**

```
msbuild QtoRevitPlugin.sln /p:Configuration="Debug R24" /t:Build /v:m
dotnet test QtoRevitPlugin.Tests --filter "FullyQualifiedName~RepositoryInterfacesTests"
```

Expected: Build succeeded + 3 test PASSED

- [ ] **Step 6: Commit**

```bash
git add QtoRevitPlugin.Core/Data/IQtoRepository.cs QtoRevitPlugin.Core/Data/IPriceListRepository.cs QtoRevitPlugin.Tests/Sprint6/RepositoryInterfacesTests.cs
git commit -m "feat(sprint6): IQtoRepository, IFavoritesRepository, IPriceListRepository interfaces"
```

---

### Task 4: Schema v4 — ChangeLog table + audit columns + ElementSnapshots

**Files:**
- Modify: `QtoRevitPlugin.Core/Data/DatabaseSchema.cs`
- Modify: `QtoRevitPlugin.Core/Data/DatabaseInitializer.cs`

- [ ] **Step 1: Scrivere il test fallente**

Crea `QtoRevitPlugin.Tests/Sprint6/AuditFieldsMigrationTests.cs`:

```csharp
using Microsoft.Data.Sqlite;
using QtoRevitPlugin.Data;
using System.IO;
using Xunit;

namespace QtoRevitPlugin.Tests.Sprint6
{
    public class AuditFieldsMigrationTests
    {
        [Fact]
        public void NewDb_HasChangeLogTable()
        {
            var path = Path.GetTempFileName();
            try
            {
                var init = new DatabaseInitializer(path);
                using var conn = init.OpenOrCreate();
                using var cmd = conn.CreateCommand();
                cmd.CommandText = "SELECT name FROM sqlite_master WHERE type='table' AND name='ChangeLog';";
                var result = cmd.ExecuteScalar();
                Assert.Equal("ChangeLog", result);
            }
            finally { File.Delete(path); }
        }

        [Fact]
        public void NewDb_QtoAssignments_HasCreatedByColumn()
        {
            var path = Path.GetTempFileName();
            try
            {
                var init = new DatabaseInitializer(path);
                using var conn = init.OpenOrCreate();
                using var cmd = conn.CreateCommand();
                cmd.CommandText = "PRAGMA table_info(QtoAssignments);";
                using var reader = cmd.ExecuteReader();
                bool found = false;
                while (reader.Read())
                    if (reader.GetString(1) == "CreatedBy") { found = true; break; }
                Assert.True(found);
            }
            finally { File.Delete(path); }
        }

        [Fact]
        public void NewDb_HasElementSnapshotsTable()
        {
            var path = Path.GetTempFileName();
            try
            {
                var init = new DatabaseInitializer(path);
                using var conn = init.OpenOrCreate();
                using var cmd = conn.CreateCommand();
                cmd.CommandText = "SELECT name FROM sqlite_master WHERE type='table' AND name='ElementSnapshots';";
                var result = cmd.ExecuteScalar();
                Assert.Equal("ElementSnapshots", result);
            }
            finally { File.Delete(path); }
        }

        [Fact]
        public void ExistingV3Db_MigratesTo_V4()
        {
            // Simula un DB esistente alla v3 (nessuna colonna audit)
            var path = Path.GetTempFileName();
            try
            {
                // Crea DB v3 simulato
                using (var conn = new SqliteConnection($"Data Source={path}"))
                {
                    conn.Open();
                    using var tx = conn.BeginTransaction();
                    conn.Execute("CREATE TABLE IF NOT EXISTS SchemaInfo (Version INTEGER PRIMARY KEY, AppliedAt DATETIME, Notes TEXT);", transaction: tx);
                    conn.Execute("CREATE TABLE IF NOT EXISTS Sessions (Id INTEGER PRIMARY KEY);", transaction: tx);
                    conn.Execute("CREATE TABLE IF NOT EXISTS QtoAssignments (Id INTEGER PRIMARY KEY, SessionId INTEGER, UniqueId TEXT, EpCode TEXT);", transaction: tx);
                    conn.Execute("INSERT INTO SchemaInfo (Version, Notes) VALUES (3, 'test');", transaction: tx);
                    tx.Commit();
                }

                // Apri con initializer → deve migrare
                var init = new DatabaseInitializer(path);
                using var migratedConn = init.OpenOrCreate();

                using var versionCmd = migratedConn.CreateCommand();
                versionCmd.CommandText = "SELECT MAX(Version) FROM SchemaInfo;";
                var version = Convert.ToInt32(versionCmd.ExecuteScalar());
                Assert.Equal(4, version);
            }
            finally { File.Delete(path); }
        }
    }
}
```

Aggiungi `using Dapper;` e `using System;` agli import del test.

- [ ] **Step 2: Esegui test per verificare fallimento**

```
dotnet test QtoRevitPlugin.Tests --filter "FullyQualifiedName~AuditFieldsMigrationTests" --no-build
```

Expected: FAIL — tabelle mancanti

- [ ] **Step 3: Aggiorna `DatabaseSchema.cs`**

Modifica il file aggiornando `CurrentVersion` e aggiungendo costanti per ChangeLog, ElementSnapshots, e migration:

```csharp
// v4 (Sprint 6): ChangeLog audit table + audit columns su QtoAssignments + ElementSnapshots
public const int CurrentVersion = 4;
```

Aggiungi nell'array `InitialStatements` dopo `ModelDiffLog`:
```csharp
ChangeLog,
ElementSnapshots,
```

Aggiungi le costanti DDL:

```csharp
public const string ChangeLog = @"
CREATE TABLE IF NOT EXISTS ChangeLog (
    ChangeId        INTEGER PRIMARY KEY AUTOINCREMENT,
    SessionId       INTEGER NOT NULL,
    ElementUniqueId TEXT NOT NULL,
    PriceItemCode   TEXT NOT NULL,
    ChangeType      TEXT NOT NULL,
    OldValueJson    TEXT,
    NewValueJson    TEXT,
    UserId          TEXT NOT NULL,
    Timestamp       TEXT NOT NULL
);
CREATE INDEX IF NOT EXISTS IX_ChangeLog_Session ON ChangeLog(SessionId);";

public const string ElementSnapshots = @"
CREATE TABLE IF NOT EXISTS ElementSnapshots (
    Id            INTEGER PRIMARY KEY AUTOINCREMENT,
    SessionId     INTEGER NOT NULL REFERENCES Sessions(Id) ON DELETE CASCADE,
    ElementId     INTEGER NOT NULL,
    UniqueId      TEXT NOT NULL,
    SnapshotHash  TEXT NOT NULL,
    SnapshotQty   REAL NOT NULL DEFAULT 0,
    AssignedEPJson TEXT NOT NULL DEFAULT '[]',
    LastUpdated   TEXT NOT NULL,
    UNIQUE(SessionId, UniqueId)
);
CREATE INDEX IF NOT EXISTS IX_ElementSnapshots_Session ON ElementSnapshots(SessionId);";
```

Aggiungi costanti per la migrazione v3→v4:

```csharp
public const string MigrateV3ToV4_CreatedBy     = "ALTER TABLE QtoAssignments ADD COLUMN CreatedBy TEXT NOT NULL DEFAULT '';";
public const string MigrateV3ToV4_CreatedAt      = "ALTER TABLE QtoAssignments ADD COLUMN CreatedAt TEXT NOT NULL DEFAULT '';";
public const string MigrateV3ToV4_ModifiedBy     = "ALTER TABLE QtoAssignments ADD COLUMN ModifiedBy TEXT;";
public const string MigrateV3ToV4_Version        = "ALTER TABLE QtoAssignments ADD COLUMN Version INTEGER NOT NULL DEFAULT 1;";
public const string MigrateV3ToV4_AuditStatus    = "ALTER TABLE QtoAssignments ADD COLUMN AuditStatus TEXT NOT NULL DEFAULT 'Active';";
```

- [ ] **Step 4: Aggiorna `DatabaseInitializer.cs` — migrazione v3→v4**

Nella method `MigrateIfNeeded`, dopo il blocco `if (dbVersion < 3 ...)`, aggiungi:

```csharp
// Migrazione v3 → v4 (Sprint 6): ChangeLog + ElementSnapshots tables + audit columns su QtoAssignments
if (dbVersion < 4)
{
    // Nuove tabelle (idempotenti con IF NOT EXISTS)
    ExecuteStatement(conn, tx, DatabaseSchema.ChangeLog);
    ExecuteStatement(conn, tx, DatabaseSchema.ElementSnapshots);

    // Audit columns (ALTER TABLE non supporta IF NOT EXISTS → check via PRAGMA)
    if (!ColumnExists(conn, tx, "QtoAssignments", "CreatedBy"))
        ExecuteStatement(conn, tx, DatabaseSchema.MigrateV3ToV4_CreatedBy);
    if (!ColumnExists(conn, tx, "QtoAssignments", "CreatedAt"))
        ExecuteStatement(conn, tx, DatabaseSchema.MigrateV3ToV4_CreatedAt);
    if (!ColumnExists(conn, tx, "QtoAssignments", "ModifiedBy"))
        ExecuteStatement(conn, tx, DatabaseSchema.MigrateV3ToV4_ModifiedBy);
    if (!ColumnExists(conn, tx, "QtoAssignments", "Version"))
        ExecuteStatement(conn, tx, DatabaseSchema.MigrateV3ToV4_Version);
    if (!ColumnExists(conn, tx, "QtoAssignments", "AuditStatus"))
        ExecuteStatement(conn, tx, DatabaseSchema.MigrateV3ToV4_AuditStatus);
}
```

Aggiungi il metodo helper `ExecuteStatement` alla classe `DatabaseInitializer`:

```csharp
private static void ExecuteStatement(SqliteConnection conn, SqliteTransaction tx, string sql)
{
    using var cmd = conn.CreateCommand();
    cmd.Transaction = tx;
    cmd.CommandText = sql;
    cmd.ExecuteNonQuery();
}
```

Aggiorna la versione inserita in `ApplyInitialSchema`:
```csharp
versionCmd.Parameters.AddWithValue("$v", DatabaseSchema.CurrentVersion);
versionCmd.Parameters.AddWithValue("$n", "Schema iniziale Sprint 1-6");
```

- [ ] **Step 5: Aggiorna `QtoAssignment.cs` — aggiungi audit fields**

Aggiungi queste proprietà alla classe `QtoAssignment`:

```csharp
// Audit fields (Sprint 6)
public string CreatedBy { get; set; } = "";
public DateTime CreatedAt { get; set; } = DateTime.UtcNow;
public string? ModifiedBy { get; set; }
public int Version { get; set; } = 1;
public AssignmentStatus AuditStatus { get; set; } = AssignmentStatus.Active;
```

- [ ] **Step 6: Build e test**

```
msbuild QtoRevitPlugin.sln /p:Configuration="Debug R24" /t:Build /v:m
dotnet test QtoRevitPlugin.Tests --filter "FullyQualifiedName~AuditFieldsMigrationTests"
```

Expected: Build succeeded + 4 test PASSED

- [ ] **Step 7: Commit**

```bash
git add QtoRevitPlugin.Core/Data/DatabaseSchema.cs QtoRevitPlugin.Core/Data/DatabaseInitializer.cs QtoRevitPlugin.Core/Models/QtoAssignment.cs QtoRevitPlugin.Tests/Sprint6/AuditFieldsMigrationTests.cs
git commit -m "feat(sprint6): schema v4 - ChangeLog + ElementSnapshots + audit columns migration"
```

---

### Task 5: QtoRepository implementa IQtoRepository — ChangeLog e Snapshots

**Files:**
- Modify: `QtoRevitPlugin.Core/Data/QtoRepository.cs`

- [ ] **Step 1: Implementa `IQtoRepository` su `QtoRepository`**

Aggiungi `: IQtoRepository` alla dichiarazione della classe:

```csharp
public class QtoRepository : IQtoRepository, IDisposable
```

- [ ] **Step 2: Aggiorna `InsertAssignment` per includere audit fields**

Trova il metodo `InsertAssignment` e aggiorna la SQL per includere le nuove colonne. Aggiungi alla INSERT:

```sql
CreatedBy, CreatedAt, ModifiedBy, Version, AuditStatus
```

E i valori corrispondenti:

```sql
@CreatedBy, @CreatedAt, @ModifiedBy, @Version, @AuditStatus
```

Nel parametro Dapper, aggiungi:

```csharp
CreatedBy = assignment.CreatedBy,
CreatedAt = assignment.CreatedAt,
ModifiedBy = assignment.ModifiedBy,
Version = assignment.Version,
AuditStatus = assignment.AuditStatus.ToString(),
```

- [ ] **Step 3: Aggiunge metodi `AppendChangeLog` e `GetChangeLog`**

```csharp
public void AppendChangeLog(ChangeLogEntry entry)
{
    const string sql = @"
        INSERT INTO ChangeLog
            (SessionId, ElementUniqueId, PriceItemCode, ChangeType, OldValueJson, NewValueJson, UserId, Timestamp)
        VALUES
            (@SessionId, @ElementUniqueId, @PriceItemCode, @ChangeType, @OldValueJson, @NewValueJson, @UserId, @Timestamp);";

    _conn.Execute(sql, new
    {
        entry.SessionId,
        entry.ElementUniqueId,
        entry.PriceItemCode,
        entry.ChangeType,
        entry.OldValueJson,
        entry.NewValueJson,
        entry.UserId,
        Timestamp = entry.Timestamp.ToString("o")
    });
}

public IReadOnlyList<ChangeLogEntry> GetChangeLog(int sessionId)
{
    const string sql = "SELECT * FROM ChangeLog WHERE SessionId = @sessionId ORDER BY ChangeId;";
    var rows = _conn.Query<dynamic>(sql, new { sessionId });
    var result = new List<ChangeLogEntry>();
    foreach (var row in rows)
    {
        result.Add(new ChangeLogEntry
        {
            ChangeId = (int)row.ChangeId,
            SessionId = (int)row.SessionId,
            ElementUniqueId = row.ElementUniqueId ?? "",
            PriceItemCode = row.PriceItemCode ?? "",
            ChangeType = row.ChangeType ?? "",
            OldValueJson = row.OldValueJson,
            NewValueJson = row.NewValueJson,
            UserId = row.UserId ?? "",
            Timestamp = DateTime.Parse(row.Timestamp ?? DateTime.UtcNow.ToString("o"))
        });
    }
    return result;
}
```

- [ ] **Step 4: Aggiunge metodi `UpsertSnapshot` e `GetSnapshots`**

```csharp
public void UpsertSnapshot(ElementSnapshot snapshot)
{
    const string sql = @"
        INSERT INTO ElementSnapshots
            (SessionId, ElementId, UniqueId, SnapshotHash, SnapshotQty, AssignedEPJson, LastUpdated)
        VALUES
            (@SessionId, @ElementId, @UniqueId, @SnapshotHash, @SnapshotQty, @AssignedEPJson, @LastUpdated)
        ON CONFLICT(SessionId, UniqueId) DO UPDATE SET
            SnapshotHash  = excluded.SnapshotHash,
            SnapshotQty   = excluded.SnapshotQty,
            AssignedEPJson = excluded.AssignedEPJson,
            LastUpdated   = excluded.LastUpdated;";

    _conn.Execute(sql, new
    {
        snapshot.SessionId,
        snapshot.ElementId,
        snapshot.UniqueId,
        snapshot.SnapshotHash,
        snapshot.SnapshotQty,
        AssignedEPJson = System.Text.Json.JsonSerializer.Serialize(snapshot.AssignedEP),
        LastUpdated = snapshot.LastUpdated.ToString("o")
    });
}

public IReadOnlyList<ElementSnapshot> GetSnapshots(int sessionId)
{
    const string sql = "SELECT * FROM ElementSnapshots WHERE SessionId = @sessionId;";
    var rows = _conn.Query<dynamic>(sql, new { sessionId });
    var result = new List<ElementSnapshot>();
    foreach (var row in rows)
    {
        var epJson = (string?)row.AssignedEPJson ?? "[]";
        result.Add(new ElementSnapshot
        {
            Id = (int)row.Id,
            SessionId = (int)row.SessionId,
            ElementId = (int)row.ElementId,
            UniqueId = row.UniqueId ?? "",
            SnapshotHash = row.SnapshotHash ?? "",
            SnapshotQty = (double)row.SnapshotQty,
            AssignedEP = System.Text.Json.JsonSerializer.Deserialize<List<string>>(epJson) ?? new(),
            LastUpdated = DateTime.Parse(row.LastUpdated ?? DateTime.UtcNow.ToString("o"))
        });
    }
    return result;
}
```

- [ ] **Step 5: `GetSession` — implementazione se mancante**

Verifica che `GetSession(int sessionId)` esista in `QtoRepository.cs`. Se manca, aggiungi:

```csharp
public WorkSession? GetSession(int sessionId)
{
    const string sql = "SELECT * FROM Sessions WHERE Id = @sessionId LIMIT 1;";
    return _conn.QueryFirstOrDefault<WorkSession>(sql, new { sessionId });
}
```

- [ ] **Step 6: Build entrambi i target**

```
msbuild QtoRevitPlugin.sln /p:Configuration="Debug R24" /t:Build /v:m
msbuild QtoRevitPlugin.sln /p:Configuration="Debug R25" /t:Build /v:m
```

Expected: entrambi Build succeeded, 0 Error(s)

- [ ] **Step 7: Commit**

```bash
git add QtoRevitPlugin.Core/Data/QtoRepository.cs
git commit -m "feat(sprint6): QtoRepository implements IQtoRepository - ChangeLog + Snapshots CRUD"
```

---

### Task 6: FileFavoritesRepository

**Files:**
- Create: `QtoRevitPlugin.Core/Data/FileFavoritesRepository.cs`
- Create: `QtoRevitPlugin.Tests/Sprint7/FavoritesRepositoryTests.cs`

- [ ] **Step 1: Scrivere i test fallenti**

```csharp
using QtoRevitPlugin.Data;
using QtoRevitPlugin.Models;
using System.IO;
using Xunit;

namespace QtoRevitPlugin.Tests.Sprint7
{
    public class FavoritesRepositoryTests
    {
        [Fact]
        public void SaveAndLoadGlobal_RoundTrips()
        {
            var tempDir = Path.Combine(Path.GetTempPath(), Path.GetRandomFileName());
            Directory.CreateDirectory(tempDir);
            try
            {
                var repo = new FileFavoritesRepository(tempDir);
                var set = new FavoriteSet
                {
                    Name = "Test",
                    Items = new() { new FavoriteItem { Code = "A.01.001", ShortDesc = "Test item", Unit = "m²", UnitPrice = 42.0 } }
                };
                repo.SaveGlobal(set);
                var loaded = repo.LoadGlobal();
                Assert.Equal("Test", loaded.Name);
                Assert.Single(loaded.Items);
                Assert.Equal("A.01.001", loaded.Items[0].Code);
            }
            finally { Directory.Delete(tempDir, recursive: true); }
        }

        [Fact]
        public void LoadGlobal_WhenMissing_ReturnsEmpty()
        {
            var tempDir = Path.Combine(Path.GetTempPath(), Path.GetRandomFileName());
            Directory.CreateDirectory(tempDir);
            try
            {
                var repo = new FileFavoritesRepository(tempDir);
                var loaded = repo.LoadGlobal();
                Assert.NotNull(loaded);
                Assert.Empty(loaded.Items);
            }
            finally { Directory.Delete(tempDir, recursive: true); }
        }

        [Fact]
        public void LoadForProject_OverridesGlobal_WhenBothPresent()
        {
            var tempDir = Path.Combine(Path.GetTempPath(), Path.GetRandomFileName());
            Directory.CreateDirectory(tempDir);
            var projectDir = Path.Combine(Path.GetTempPath(), Path.GetRandomFileName());
            Directory.CreateDirectory(projectDir);
            var cmePath = Path.Combine(projectDir, "test.cme");
            try
            {
                var repo = new FileFavoritesRepository(tempDir);
                var globalSet = new FavoriteSet { Name = "Globale" };
                var projectSet = new FavoriteSet { Name = "Progetto" };
                repo.SaveGlobal(globalSet);
                repo.SaveForProject(cmePath, projectSet);

                var loaded = repo.LoadForProject(cmePath);
                Assert.Equal("Progetto", loaded?.Name);
            }
            finally
            {
                Directory.Delete(tempDir, recursive: true);
                Directory.Delete(projectDir, recursive: true);
            }
        }
    }
}
```

- [ ] **Step 2: Esegui test per verificare fallimento**

```
dotnet test QtoRevitPlugin.Tests --filter "FullyQualifiedName~FavoritesRepositoryTests" --no-build
```

Expected: FAIL — tipo non trovato

- [ ] **Step 3: Crea `FileFavoritesRepository.cs`**

```csharp
using QtoRevitPlugin.Models;
using System;
using System.IO;
using System.Text.Json;

namespace QtoRevitPlugin.Data
{
    public class FileFavoritesRepository : IFavoritesRepository
    {
        private static readonly JsonSerializerOptions JsonOptions = new() { WriteIndented = true };

        private readonly string _globalDir;

        public FileFavoritesRepository(string globalDir)
        {
            _globalDir = globalDir ?? throw new ArgumentNullException(nameof(globalDir));
        }

        public static string GetDefaultGlobalDir()
        {
            var appData = Environment.GetFolderPath(Environment.SpecialFolder.ApplicationData);
            return Path.Combine(appData, "CmePlugin", "Favorites");
        }

        public FavoriteSet LoadGlobal()
        {
            var path = Path.Combine(_globalDir, "default.json");
            return LoadFromFile(path) ?? new FavoriteSet();
        }

        public void SaveGlobal(FavoriteSet set)
        {
            Directory.CreateDirectory(_globalDir);
            var path = Path.Combine(_globalDir, "default.json");
            File.WriteAllText(path, JsonSerializer.Serialize(set, JsonOptions));
        }

        public FavoriteSet? LoadForProject(string cmePath)
        {
            var dir = Path.GetDirectoryName(cmePath);
            if (string.IsNullOrEmpty(dir)) return null;
            var path = Path.Combine(dir, "favorites.json");
            return LoadFromFile(path);
        }

        public void SaveForProject(string cmePath, FavoriteSet set)
        {
            var dir = Path.GetDirectoryName(cmePath);
            if (string.IsNullOrEmpty(dir)) return;
            Directory.CreateDirectory(dir);
            var path = Path.Combine(dir, "favorites.json");
            File.WriteAllText(path, JsonSerializer.Serialize(set, JsonOptions));
        }

        private static FavoriteSet? LoadFromFile(string path)
        {
            if (!File.Exists(path)) return null;
            try
            {
                var json = File.ReadAllText(path);
                return JsonSerializer.Deserialize<FavoriteSet>(json);
            }
            catch
            {
                return null;
            }
        }
    }
}
```

- [ ] **Step 4: Build e test**

```
msbuild QtoRevitPlugin.sln /p:Configuration="Debug R24" /t:Build /v:m
dotnet test QtoRevitPlugin.Tests --filter "FullyQualifiedName~FavoritesRepositoryTests"
```

Expected: Build succeeded + 3 test PASSED

- [ ] **Step 5: Commit**

```bash
git add QtoRevitPlugin.Core/Data/FileFavoritesRepository.cs QtoRevitPlugin.Tests/Sprint7/FavoritesRepositoryTests.cs
git commit -m "feat(sprint6): FileFavoritesRepository - persistenza Preferiti su filesystem JSON"
```

---

### Task 7: MappingRulesService

**Files:**
- Create: `QtoRevitPlugin.Core/Services/MappingRulesService.cs`
- Create: `QtoRevitPlugin.Tests/Sprint7/MappingRulesServiceTests.cs`

- [ ] **Step 1: Scrivere i test fallenti**

```csharp
using QtoRevitPlugin.Services;
using System.IO;
using Xunit;

namespace QtoRevitPlugin.Tests.Sprint7
{
    public class MappingRulesServiceTests
    {
        [Fact]
        public void GetRule_ForWall_ReturnsAreaDefault()
        {
            var tempDir = Path.Combine(Path.GetTempPath(), Path.GetRandomFileName());
            Directory.CreateDirectory(tempDir);
            try
            {
                var svc = new MappingRulesService(globalDir: tempDir);
                var rule = svc.GetRule("OST_Walls");
                Assert.Equal("Area", rule.DefaultParam);
                Assert.Contains("Area", rule.AllowedParams);
            }
            finally { Directory.Delete(tempDir, recursive: true); }
        }

        [Fact]
        public void GetRule_ForUnknownCategory_ReturnsFallback()
        {
            var tempDir = Path.Combine(Path.GetTempPath(), Path.GetRandomFileName());
            Directory.CreateDirectory(tempDir);
            try
            {
                var svc = new MappingRulesService(globalDir: tempDir);
                var rule = svc.GetRule("OST_FurnitureSystems");
                Assert.Equal("Count", rule.DefaultParam);
            }
            finally { Directory.Delete(tempDir, recursive: true); }
        }

        [Fact]
        public void SaveAndLoad_RoundTrips()
        {
            var tempDir = Path.Combine(Path.GetTempPath(), Path.GetRandomFileName());
            Directory.CreateDirectory(tempDir);
            try
            {
                var svc = new MappingRulesService(globalDir: tempDir);
                var config = svc.LoadGlobal();
                config.Rules[0].RoundingDecimals = 3;
                svc.SaveGlobal(config);

                var svc2 = new MappingRulesService(globalDir: tempDir);
                var reloaded = svc2.LoadGlobal();
                Assert.Equal(3, reloaded.Rules[0].RoundingDecimals);
            }
            finally { Directory.Delete(tempDir, recursive: true); }
        }
    }
}
```

- [ ] **Step 2: Esegui test per verificare fallimento**

```
dotnet test QtoRevitPlugin.Tests --filter "FullyQualifiedName~MappingRulesServiceTests" --no-build
```

Expected: FAIL — tipo non trovato

- [ ] **Step 3: Crea `MappingRulesService.cs`**

```csharp
using QtoRevitPlugin.Models;
using System;
using System.Collections.Generic;
using System.IO;
using System.Text.Json;

namespace QtoRevitPlugin.Services
{
    public class MappingRulesService
    {
        private static readonly JsonSerializerOptions JsonOptions = new() { WriteIndented = true };
        private const string FileName = "QTO_MappingRules.json";

        private readonly string _globalDir;
        private string? _projectCmePath;

        private static readonly MappingRulesConfig DefaultConfig = new()
        {
            Version = 1,
            Rules = new List<MappingRule>
            {
                new() { RevitCategory = "OST_Walls",            DefaultParam = "Area",   AllowedParams = new(){"Area","Volume","Length","Count"}, HashParams = new(){"Area","Volume"},  UnitDisplay = "m²", RoundingDecimals = 2, VuotoPerPieno = true  },
                new() { RevitCategory = "OST_Floors",           DefaultParam = "Area",   AllowedParams = new(){"Area","Volume","Count"},           HashParams = new(){"Area","Volume"},  UnitDisplay = "m²", RoundingDecimals = 2, VuotoPerPieno = false },
                new() { RevitCategory = "OST_Ceilings",         DefaultParam = "Area",   AllowedParams = new(){"Area","Count"},                   HashParams = new(){"Area"},           UnitDisplay = "m²", RoundingDecimals = 2, VuotoPerPieno = false },
                new() { RevitCategory = "OST_Roofs",            DefaultParam = "Area",   AllowedParams = new(){"Area","Volume","Count"},           HashParams = new(){"Area","Volume"},  UnitDisplay = "m²", RoundingDecimals = 2, VuotoPerPieno = false },
                new() { RevitCategory = "OST_StructuralFraming",DefaultParam = "Length", AllowedParams = new(){"Length","Count"},                 HashParams = new(){"Length"},         UnitDisplay = "m",  RoundingDecimals = 2, VuotoPerPieno = false },
                new() { RevitCategory = "OST_StructuralColumns", DefaultParam = "Volume", AllowedParams = new(){"Volume","Length","Count"},        HashParams = new(){"Volume","Length"},UnitDisplay = "m³", RoundingDecimals = 3, VuotoPerPieno = false },
                new() { RevitCategory = "OST_Doors",            DefaultParam = "Count",  AllowedParams = new(){"Count"},                          HashParams = new(){"Count"},          UnitDisplay = "nr", RoundingDecimals = 0, VuotoPerPieno = false },
                new() { RevitCategory = "OST_Windows",          DefaultParam = "Count",  AllowedParams = new(){"Count"},                          HashParams = new(){"Count"},          UnitDisplay = "nr", RoundingDecimals = 0, VuotoPerPieno = false },
            }
        };

        private static readonly MappingRule FallbackRule = new()
        {
            RevitCategory = "*",
            DefaultParam = "Count",
            AllowedParams = new() { "Count" },
            HashParams = new() { "Count" },
            UnitDisplay = "nr",
            RoundingDecimals = 0
        };

        public MappingRulesService(string? globalDir = null, string? projectCmePath = null)
        {
            _globalDir = globalDir ?? GetDefaultGlobalDir();
            _projectCmePath = projectCmePath;
        }

        public static string GetDefaultGlobalDir()
        {
            var appData = Environment.GetFolderPath(Environment.SpecialFolder.ApplicationData);
            return Path.Combine(appData, "CmePlugin");
        }

        public void SetProjectCmePath(string cmePath) => _projectCmePath = cmePath;

        public MappingRulesConfig LoadGlobal()
        {
            var path = Path.Combine(_globalDir, FileName);
            if (!File.Exists(path)) return Clone(DefaultConfig);
            try
            {
                var json = File.ReadAllText(path);
                return JsonSerializer.Deserialize<MappingRulesConfig>(json) ?? Clone(DefaultConfig);
            }
            catch { return Clone(DefaultConfig); }
        }

        public void SaveGlobal(MappingRulesConfig config)
        {
            Directory.CreateDirectory(_globalDir);
            File.WriteAllText(Path.Combine(_globalDir, FileName), JsonSerializer.Serialize(config, JsonOptions));
        }

        public MappingRulesConfig? LoadForProject()
        {
            if (string.IsNullOrEmpty(_projectCmePath)) return null;
            var dir = Path.GetDirectoryName(_projectCmePath);
            if (string.IsNullOrEmpty(dir)) return null;
            var path = Path.Combine(dir, FileName);
            if (!File.Exists(path)) return null;
            try
            {
                var json = File.ReadAllText(path);
                return JsonSerializer.Deserialize<MappingRulesConfig>(json);
            }
            catch { return null; }
        }

        public MappingRule GetRule(string revitCategory)
        {
            var config = LoadForProject() ?? LoadGlobal();
            foreach (var rule in config.Rules)
                if (string.Equals(rule.RevitCategory, revitCategory, StringComparison.OrdinalIgnoreCase))
                    return rule;
            return FallbackRule;
        }

        private static MappingRulesConfig Clone(MappingRulesConfig src)
        {
            var json = JsonSerializer.Serialize(src, JsonOptions);
            return JsonSerializer.Deserialize<MappingRulesConfig>(json)!;
        }
    }
}
```

- [ ] **Step 4: Build e test**

```
msbuild QtoRevitPlugin.sln /p:Configuration="Debug R24" /t:Build /v:m
dotnet test QtoRevitPlugin.Tests --filter "FullyQualifiedName~MappingRulesServiceTests"
```

Expected: Build succeeded + 3 test PASSED

- [ ] **Step 5: Commit**

```bash
git add QtoRevitPlugin.Core/Services/MappingRulesService.cs QtoRevitPlugin.Tests/Sprint7/MappingRulesServiceTests.cs
git commit -m "feat(sprint6-7): MappingRulesService - carica QTO_MappingRules.json globale/progetto con default integrati"
```

---

## Sprint 7 — UI Prezzario + Preferiti + MappingRules

### Task 8: IUserContext registrato in QtoApplication + secondo pulsante ribbon

**Files:**
- Modify: `QtoRevitPlugin/Application/QtoApplication.cs`

- [ ] **Step 1: Aggiungi proprietà `UserContext` e `CatalogBrowser` a `QtoApplication`**

Aggiungi dopo `public UserLibraryManager UserLibrary { get; private set; } = null!;`:

```csharp
public IUserContext UserContext { get; private set; } = null!;
public CatalogBrowserWindow? CatalogBrowser { get; private set; }
```

Aggiungi l'using mancante in cima al file:

```csharp
using QtoRevitPlugin.Services;
using QtoRevitPlugin.UI.Views; // per CatalogBrowserWindow quando aggiunta
```

- [ ] **Step 2: Inizializza `UserContext` in `OnStartup`, prima di `SessionManager`**

In `OnStartup`, subito dopo `UserLibrary = new UserLibraryManager();`, aggiungi:

```csharp
CrashLogger.Info("→ IUserContext");
UserContext = new WindowsUserContext();
```

- [ ] **Step 3: Modifica `CreateRibbon` per aggiungere il toggle Prezzario**

Sostituisci il metodo `CreateRibbon` con:

```csharp
private static void CreateRibbon(UIControlledApplication application)
{
    try { application.CreateRibbonTab(QtoConstants.RibbonTabName); }
    catch { /* già esistente */ }

    var assemblyPath = Assembly.GetExecutingAssembly().Location;
    RibbonPanel panel = application.CreateRibbonPanel(QtoConstants.RibbonTabName, QtoConstants.RibbonPanelName);

    var launchButton = new PushButtonData(
        "LaunchCme",
        "Avvia CME",
        assemblyPath,
        "QtoRevitPlugin.Commands.LaunchQtoCommand")
    {
        ToolTip = "Apre il pannello CME – Computo Metrico Estimativo",
        LargeImage = IconFactory.CreateLaunchIcon(32),
        Image = IconFactory.CreateLaunchIcon(16)
    };

    var prezzarioButton = new PushButtonData(
        "TogglePrezzario",
        "Prezzario",
        assemblyPath,
        "QtoRevitPlugin.Commands.ToggleCatalogBrowserCommand")
    {
        ToolTip = "Apre/chiude il pannello Prezzario con Preferiti e voci EP"
    };

    panel.AddItem(launchButton);
    panel.AddSeparator();
    panel.AddItem(prezzarioButton);
}
```

- [ ] **Step 4: Build Debug R24 per verifica**

```
msbuild QtoRevitPlugin.sln /p:Configuration="Debug R24" /t:Build /v:m
```

Expected: eventuali errori solo sul comando mancante — accettabili fino al task successivo

- [ ] **Step 5: Commit**

```bash
git add QtoRevitPlugin/Application/QtoApplication.cs
git commit -m "feat(sprint7): QtoApplication - IUserContext + ribbon button Prezzario"
```

---

### Task 9: ToggleCatalogBrowserCommand + CatalogBrowserWindow singleton

**Files:**
- Create: `QtoRevitPlugin/Commands/ToggleCatalogBrowserCommand.cs`
- Modify: `QtoRevitPlugin/UI/CatalogBrowserWindow.xaml.cs`

- [ ] **Step 1: Crea `ToggleCatalogBrowserCommand.cs`**

```csharp
using Autodesk.Revit.Attributes;
using Autodesk.Revit.DB;
using Autodesk.Revit.UI;
using QtoRevitPlugin.Application;
using QtoRevitPlugin.UI;
using Revit.Async;
using System;

namespace QtoRevitPlugin.Commands
{
    [Transaction(TransactionMode.ReadOnly)]
    [Regeneration(RegenerationOption.Manual)]
    public class ToggleCatalogBrowserCommand : IExternalCommand
    {
        public Result Execute(ExternalCommandData commandData, ref string message, ElementSet elements)
        {
            try
            {
                var app = QtoApplication.Instance;

                // Crea singleton CatalogBrowserWindow al primo click
                if (app.CatalogBrowser == null)
                {
                    var window = new CatalogBrowserWindow();
                    window.Closed += (_, _) =>
                    {
                        // Resetta singleton quando l'utente chiude la finestra con X
                        // (la prossima apertura via ribbon ricrea la window)
                        // Non fare nulla: manteniamo il riferimento per Hide/Show
                    };
                    // Imposta come finestra non modale senza owner (finestra separata)
                    window.Show();
                    SetCatalogBrowser(app, window);
                }
                else
                {
                    if (app.CatalogBrowser.IsVisible)
                        app.CatalogBrowser.Hide();
                    else
                        app.CatalogBrowser.Show();
                }

                return Result.Succeeded;
            }
            catch (Exception ex)
            {
                CrashLogger.WriteException("ToggleCatalogBrowserCommand", ex);
                message = ex.Message;
                return Result.Failed;
            }
        }

        private static void SetCatalogBrowser(QtoApplication app, CatalogBrowserWindow window)
        {
            // Usa reflection per impostare la proprietà privata (o rendi setter internal)
            // Alternativa: rendi CatalogBrowser setter internal in QtoApplication
            typeof(QtoApplication)
                .GetProperty(nameof(QtoApplication.CatalogBrowser))!
                .SetValue(app, window);
        }
    }
}
```

**Nota:** Per evitare la reflection, rendi `CatalogBrowser` setter `internal` in `QtoApplication.cs`:

```csharp
public CatalogBrowserWindow? CatalogBrowser { get; internal set; }
```

Poi semplifica `ToggleCatalogBrowserCommand` usando:

```csharp
app.CatalogBrowser = window;
```

- [ ] **Step 2: Aggiorna `CatalogBrowserWindow.xaml.cs` — non chiude più su X, usa Hide**

Nel code-behind esistente, aggiungi override di `OnClosing`:

```csharp
protected override void OnClosing(System.ComponentModel.CancelEventArgs e)
{
    // Non chiudere la finestra: nascondi e annulla la chiusura.
    // La finestra è un singleton gestito da QtoApplication.
    e.Cancel = true;
    Hide();
}
```

- [ ] **Step 3: Build Debug R24**

```
msbuild QtoRevitPlugin.sln /p:Configuration="Debug R24" /t:Build /v:m
```

Expected: Build succeeded, 0 Error(s)

- [ ] **Step 4: Commit**

```bash
git add QtoRevitPlugin/Commands/ToggleCatalogBrowserCommand.cs QtoRevitPlugin/UI/CatalogBrowserWindow.xaml.cs
git commit -m "feat(sprint7): ToggleCatalogBrowserCommand + CatalogBrowserWindow singleton hide/show"
```

---

### Task 10: Sezione Preferiti in CatalogBrowserWindow — XAML + ViewModel

**Files:**
- Modify: `QtoRevitPlugin/UI/CatalogBrowserWindow.xaml`
- Modify: `QtoRevitPlugin/UI/ViewModels/CatalogBrowserViewModel.cs`

- [ ] **Step 1: Aggiorna `CatalogBrowserViewModel.cs` — proprietà Preferiti + ActiveItem + MappingRule**

Aggiungi al ViewModel le seguenti proprietà usando CommunityToolkit.Mvvm:

```csharp
using CommunityToolkit.Mvvm.ComponentModel;
using CommunityToolkit.Mvvm.Input;
using QtoRevitPlugin.Data;
using QtoRevitPlugin.Models;
using System.Collections.ObjectModel;
using System.Windows.Input;

// Nelle proprietà [ObservableProperty]:
[ObservableProperty] private ObservableCollection<FavoriteItem> _favorites = new();
[ObservableProperty] private FavoriteItem? _activeFavoriteItem;
[ObservableProperty] private string _selectedQuantityParam = "Count";
[ObservableProperty] private ObservableCollection<string> _allowedParams = new() { "Area", "Volume", "Length", "Count" };
[ObservableProperty] private bool _canAssign;

// Servizi iniettati (aggiungi al costruttore)
private readonly IFavoritesRepository _favoritesRepo;
private readonly MappingRulesService _mappingRulesService;
```

Aggiungi costruttore aggiornato (mantieni compatibilità con costruttore esistente):

```csharp
public CatalogBrowserViewModel(
    IFavoritesRepository favoritesRepo,
    MappingRulesService mappingRulesService)
{
    _favoritesRepo = favoritesRepo;
    _mappingRulesService = mappingRulesService;
    LoadFavorites();
}
```

Aggiungi metodi:

```csharp
private void LoadFavorites()
{
    var set = _favoritesRepo.LoadGlobal();
    Favorites.Clear();
    foreach (var item in set.Items)
        Favorites.Add(item);
}

public void AddToFavorites(FavoriteItem item)
{
    if (!Favorites.Contains(item))
        Favorites.Add(item);
    SaveFavorites();
}

public void RemoveFromFavorites(FavoriteItem item)
{
    Favorites.Remove(item);
    SaveFavorites();
}

private void SaveFavorites()
{
    var set = new FavoriteSet { Items = new(Favorites) };
    _favoritesRepo.SaveGlobal(set);
}

public void OnRevitSelectionChanged(string revitCategory)
{
    var rule = _mappingRulesService.GetRule(revitCategory);
    SelectedQuantityParam = rule.DefaultParam;
    AllowedParams = new ObservableCollection<string>(rule.AllowedParams);
}

partial void OnActiveFavoriteItemChanged(FavoriteItem? value)
    => UpdateCanAssign();

private void UpdateCanAssign()
    => CanAssign = ActiveFavoriteItem != null; // sarà AND con selezione Revit > 0
```

- [ ] **Step 2: Aggiorna `CatalogBrowserWindow.xaml` — aggiunge sezione Preferiti**

Nella grid principale del XAML, prima del TreeView esistente, aggiungi la sezione:

```xml
<!-- Sezione Preferiti -->
<GroupBox Header="⭐ Preferiti" Margin="0,0,0,8">
    <StackPanel>
        <ListBox x:Name="FavoritesList"
                 ItemsSource="{Binding Favorites}"
                 SelectedItem="{Binding ActiveFavoriteItem}"
                 Height="120">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Code}" FontWeight="Bold" Width="80"/>
                        <TextBlock Text="{Binding ShortDesc}" Width="150"/>
                        <TextBlock Text="{Binding UnitPrice, StringFormat=€ {0:N2}}" Width="70"/>
                        <TextBlock Text="{Binding Unit}" Foreground="Gray"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
            <Button Content="+ Aggiungi" Command="{Binding AddSelectedToFavoritesCommand}" Margin="0,0,4,0"/>
            <Button Content="Rimuovi" Command="{Binding RemoveFavoriteCommand}"/>
        </StackPanel>
    </StackPanel>
</GroupBox>

<!-- Parametro quantitativo + ASSEGNA -->
<StackPanel Orientation="Horizontal" Margin="0,4,0,4">
    <TextBlock Text="Param quantitativo:" VerticalAlignment="Center" Margin="0,0,8,0"/>
    <ComboBox ItemsSource="{Binding AllowedParams}"
              SelectedItem="{Binding SelectedQuantityParam}"
              Width="120"/>
</StackPanel>
<Button Content="✓ ASSEGNA AGLI ELEMENTI SELEZIONATI"
        Command="{Binding AssignCommand}"
        IsEnabled="{Binding CanAssign}"
        Background="#1E6FD9" Foreground="White"
        FontWeight="Bold" Padding="8,4" Margin="0,0,0,8"/>
```

- [ ] **Step 3: Build Debug R24**

```
msbuild QtoRevitPlugin.sln /p:Configuration="Debug R24" /t:Build /v:m
```

Expected: Build succeeded (o errori su Command mancanti — correggibili nel passo successivo)

- [ ] **Step 4: Commit**

```bash
git add QtoRevitPlugin/UI/CatalogBrowserWindow.xaml QtoRevitPlugin/UI/ViewModels/CatalogBrowserViewModel.cs
git commit -m "feat(sprint7): UI Preferiti in CatalogBrowserWindow + MappingRule dropdown + ASSEGNA button"
```

---

### Task 11: Flusso ASSEGNA — comando con ChangeLog

**Files:**
- Modify: `QtoRevitPlugin/UI/ViewModels/CatalogBrowserViewModel.cs`

- [ ] **Step 1: Aggiunge `AssignCommand` al ViewModel**

Il comando `AssignCommand` deve:
1. Leggere gli elementi selezionati in Revit via `RevitTask.RunAsync`
2. Chiamare `QuantityExtractor.Extract` per ogni elemento
3. Inserire `QtoAssignment` via `IQtoRepository`
4. Appendere `ChangeLogEntry` via `IQtoRepository`

Aggiungi al ViewModel:

```csharp
private readonly IQtoRepository _qtoRepository;
private readonly IUserContext _userContext;

[RelayCommand(CanExecute = nameof(CanAssign))]
private async Task AssignAsync()
{
    if (ActiveFavoriteItem == null) return;

    var uiApp = QtoRevitPlugin.Application.QtoApplication.Instance.CurrentUiApp;
    if (uiApp == null) return;

    await Revit.Async.RevitTask.RunAsync(app =>
    {
        var doc = app.ActiveUIDocument?.Document;
        var selection = app.ActiveUIDocument?.Selection;
        if (doc == null || selection == null) return;

        var selectedIds = selection.GetElementIds();
        if (selectedIds.Count == 0) return;

        var extractor = new QtoRevitPlugin.Extraction.QuantityExtractor();
        var sessionId = QtoRevitPlugin.Application.QtoApplication.Instance.SessionManager.CurrentSession?.Id ?? 0;
        var now = DateTime.UtcNow;
        var userId = _userContext.UserId;

        foreach (var elemId in selectedIds)
        {
            var elem = doc.GetElement(elemId);
            if (elem == null) continue;

            var qty = extractor.Extract(elem, SelectedQuantityParam, out var error);

            var assignment = new QtoRevitPlugin.Models.QtoAssignment
            {
                SessionId = sessionId,
                ElementId = elemId.Value,   // net8: .Value; net48: .IntegerValue (usa conditional)
                UniqueId = elem.UniqueId,
                Category = elem.Category?.Name ?? "",
                EpCode = ActiveFavoriteItem.Code,
                EpDescription = ActiveFavoriteItem.ShortDesc,
                Quantity = qty,
                Unit = ActiveFavoriteItem.Unit,
                UnitPrice = ActiveFavoriteItem.UnitPrice,
                RuleApplied = SelectedQuantityParam,
                Source = QtoRevitPlugin.Models.QtoSource.RevitElement,
                CreatedBy = userId,
                CreatedAt = now,
                AuditStatus = QtoRevitPlugin.Models.AssignmentStatus.Active
            };

            var assignId = _qtoRepository.InsertAssignment(assignment);

            _qtoRepository.AppendChangeLog(new QtoRevitPlugin.Models.ChangeLogEntry
            {
                SessionId = sessionId,
                ElementUniqueId = elem.UniqueId,
                PriceItemCode = ActiveFavoriteItem.Code,
                ChangeType = "Created",
                NewValueJson = System.Text.Json.JsonSerializer.Serialize(new { qty, param = SelectedQuantityParam }),
                UserId = userId,
                Timestamp = now
            });
        }
    });
}
```

**Nota cross-target per `ElementId.Value` vs `.IntegerValue`:**

```csharp
#if REVIT2024_OR_EARLIER
    ElementId = elemId.IntegerValue,
#else
    ElementId = (int)elemId.Value,
#endif
```

- [ ] **Step 2: Build entrambi i target**

```
msbuild QtoRevitPlugin.sln /p:Configuration="Debug R24" /t:Build /v:m
msbuild QtoRevitPlugin.sln /p:Configuration="Debug R25" /t:Build /v:m
```

Expected: entrambi Build succeeded, 0 Error(s)

- [ ] **Step 3: Commit**

```bash
git add QtoRevitPlugin/UI/ViewModels/CatalogBrowserViewModel.cs
git commit -m "feat(sprint7): AssignCommand - assegna EP agli elementi selezionati con ChangeLog audit"
```

---

## Sprint 8 — Model Diff Check

### Task 12: ModelDiffService con SHA256

**Files:**
- Create: `QtoRevitPlugin/Services/ModelDiffService.cs`
- Create: `QtoRevitPlugin.Tests/Sprint8/ModelDiffServiceTests.cs`

- [ ] **Step 1: Scrivere i test fallenti**

```csharp
using QtoRevitPlugin.Models;
using QtoRevitPlugin.Services;
using System.Collections.Generic;
using Xunit;

namespace QtoRevitPlugin.Tests.Sprint8
{
    public class ModelDiffServiceTests
    {
        [Fact]
        public void ComputeHash_SameInput_SameHash()
        {
            var hash1 = ModelDiffService.ComputeHashStatic("elem-001", new List<(string, double)> { ("Area", 12.5) });
            var hash2 = ModelDiffService.ComputeHashStatic("elem-001", new List<(string, double)> { ("Area", 12.5) });
            Assert.Equal(hash1, hash2);
        }

        [Fact]
        public void ComputeHash_DifferentArea_DifferentHash()
        {
            var hash1 = ModelDiffService.ComputeHashStatic("elem-001", new List<(string, double)> { ("Area", 12.5) });
            var hash2 = ModelDiffService.ComputeHashStatic("elem-001", new List<(string, double)> { ("Area", 13.0) });
            Assert.NotEqual(hash1, hash2);
        }

        [Fact]
        public void ComputeHash_IsTwelveHexChars()
        {
            var hash = ModelDiffService.ComputeHashStatic("elem-001", new List<(string, double)> { ("Area", 5.0) });
            Assert.Equal(12, hash.Length);
            Assert.Matches("^[0-9A-F]+$", hash);
        }
    }
}
```

- [ ] **Step 2: Esegui test per verificare fallimento**

```
dotnet test QtoRevitPlugin.Tests --filter "FullyQualifiedName~ModelDiffServiceTests" --no-build
```

Expected: FAIL — tipo non trovato

- [ ] **Step 3: Crea `ModelDiffService.cs`**

```csharp
using Autodesk.Revit.DB;
using QtoRevitPlugin.Models;
using System;
using System.Collections.Generic;
using System.Security.Cryptography;
using System.Text;

namespace QtoRevitPlugin.Services
{
    public class ModelDiffService
    {
        private readonly MappingRulesService _mappingRules;

        public ModelDiffService(MappingRulesService mappingRules)
        {
            _mappingRules = mappingRules;
        }

        public ModelDiffResult ComputeDiff(
            Document doc,
            IReadOnlyList<ElementSnapshot> snapshots,
            MappingRulesService rules)
        {
            var result = new ModelDiffResult();
            var processedUniqueIds = new HashSet<string>();

            foreach (var snap in snapshots)
            {
                processedUniqueIds.Add(snap.UniqueId);

                // Cerca elemento nel modello corrente
                var elem = doc.GetElement(snap.UniqueId);

                if (elem == null)
                {
                    // Elemento eliminato dal modello
                    result.Deleted.Add(new DiffEntry
                    {
                        Snapshot = snap,
                        CurrentElement = null,
                        OldQty = snap.SnapshotQty,
                        NewQty = 0
                    });
                    continue;
                }

                // Calcola hash corrente
                var catName = elem.Category?.Name ?? "";
                var rule = rules.GetRule(elem.Category?.Id != null
                    ? GetOstString(elem.Category)
                    : catName);

                var paramValues = ExtractHashParams(elem, rule);
                var currentHash = ComputeHashStatic(snap.UniqueId, paramValues);

                if (!string.Equals(currentHash, snap.SnapshotHash, StringComparison.OrdinalIgnoreCase))
                {
                    // Elemento modificato
                    var currentQty = ExtractPrimaryQty(elem, rule);
                    result.Modified.Add(new DiffEntry
                    {
                        Snapshot = snap,
                        CurrentElement = elem,
                        OldQty = snap.SnapshotQty,
                        NewQty = currentQty
                    });
                }
            }

            return result;
        }

        /// <summary>Calcola SHA256 troncato a 12 hex. Usato sia da ModelDiffService che da AssignCommand.</summary>
        public static string ComputeHashStatic(string uniqueId, List<(string paramName, double value)> paramValues)
        {
            var sb = new StringBuilder();
            sb.Append(uniqueId);
            foreach (var (name, value) in paramValues)
            {
                sb.Append(name);
                sb.Append(value.ToString("F6"));
            }

            var bytes = SHA256.HashData(Encoding.UTF8.GetBytes(sb.ToString()));
            return Convert.ToHexString(bytes)[..12];
        }

        private List<(string, double)> ExtractHashParams(Element elem, MappingRule rule)
        {
            var result = new List<(string, double)>();
            foreach (var paramName in rule.HashParams)
            {
                double value = 0;
                var p = elem.LookupParameter(paramName)
                    ?? elem.get_Parameter(BuiltInParameter.HOST_AREA_COMPUTED);
                if (p?.HasValue == true)
                    value = p.AsDouble();
                result.Add((paramName, value));
            }
            return result;
        }

        private double ExtractPrimaryQty(Element elem, MappingRule rule)
        {
            var p = elem.LookupParameter(rule.DefaultParam)
                ?? elem.get_Parameter(BuiltInParameter.HOST_AREA_COMPUTED);
            return p?.HasValue == true ? p.AsDouble() : 0;
        }

        private static string GetOstString(Category cat)
        {
            try { return $"OST_{cat.Name}"; }
            catch { return ""; }
        }
    }

    public class ModelDiffResult
    {
        public List<DiffEntry> Deleted { get; } = new();
        public List<DiffEntry> Modified { get; } = new();
        public List<Element> Added { get; } = new();
    }

    public class DiffEntry
    {
        public ElementSnapshot Snapshot { get; set; } = null!;
        public Element? CurrentElement { get; set; }
        public double OldQty { get; set; }
        public double NewQty { get; set; }
        public string Delta => $"{NewQty - OldQty:+0.##;-0.##;0}";
    }
}
```

- [ ] **Step 4: Build e test**

```
msbuild QtoRevitPlugin.sln /p:Configuration="Debug R24" /t:Build /v:m
dotnet test QtoRevitPlugin.Tests --filter "FullyQualifiedName~ModelDiffServiceTests"
```

Expected: Build succeeded + 3 test PASSED (i test usano `ComputeHashStatic` che non richiede Revit)

- [ ] **Step 5: Commit**

```bash
git add QtoRevitPlugin/Services/ModelDiffService.cs QtoRevitPlugin.Tests/Sprint8/ModelDiffServiceTests.cs
git commit -m "feat(sprint8): ModelDiffService - SHA256 hash + ComputeDiff logica Deleted/Modified"
```

---

### Task 13: Snapshot salvato ad ogni assegnazione

**Files:**
- Modify: `QtoRevitPlugin/UI/ViewModels/CatalogBrowserViewModel.cs`

- [ ] **Step 1: Aggiungi salvataggio snapshot in `AssignAsync`**

Dopo l'`InsertAssignment` nella loop in `AssignAsync`, aggiungi il salvataggio dello snapshot:

```csharp
// Calcola hash per snapshot
var catOst = elem.Category?.Name != null
    ? $"OST_{elem.Category.Name}"
    : "";
var rule = mappingRulesService.GetRule(catOst);
var paramValues = new List<(string, double)>();
foreach (var hp in rule.HashParams)
{
    var p = elem.LookupParameter(hp) ?? elem.get_Parameter(Autodesk.Revit.DB.BuiltInParameter.HOST_AREA_COMPUTED);
    paramValues.Add((hp, p?.HasValue == true ? p.AsDouble() : 0));
}
var hash = QtoRevitPlugin.Services.ModelDiffService.ComputeHashStatic(elem.UniqueId, paramValues);

_qtoRepository.UpsertSnapshot(new QtoRevitPlugin.Models.ElementSnapshot
{
    SessionId = sessionId,
#if REVIT2024_OR_EARLIER
    ElementId = elemId.IntegerValue,
#else
    ElementId = (int)elemId.Value,
#endif
    UniqueId = elem.UniqueId,
    SnapshotHash = hash,
    SnapshotQty = qty,
    AssignedEP = new List<string> { ActiveFavoriteItem.Code },
    LastUpdated = now
});
```

Aggiungi `mappingRulesService` come campo nel ViewModel (già iniettato come `_mappingRulesService`).

- [ ] **Step 2: Build entrambi i target**

```
msbuild QtoRevitPlugin.sln /p:Configuration="Debug R24" /t:Build /v:m
msbuild QtoRevitPlugin.sln /p:Configuration="Debug R25" /t:Build /v:m
```

Expected: entrambi Build succeeded, 0 Error(s)

- [ ] **Step 3: Commit**

```bash
git add QtoRevitPlugin/UI/ViewModels/CatalogBrowserViewModel.cs
git commit -m "feat(sprint8): salva ElementSnapshot con SHA256 ad ogni assegnazione EP"
```

---

### Task 14: ReconciliationViewModel + ReconciliationWindow

**Files:**
- Create: `QtoRevitPlugin/UI/ViewModels/ReconciliationViewModel.cs`
- Create: `QtoRevitPlugin/UI/Views/ReconciliationWindow.xaml`
- Create: `QtoRevitPlugin/UI/Views/ReconciliationWindow.xaml.cs`

- [ ] **Step 1: Crea `ReconciliationViewModel.cs`**

```csharp
using CommunityToolkit.Mvvm.ComponentModel;
using CommunityToolkit.Mvvm.Input;
using QtoRevitPlugin.Data;
using QtoRevitPlugin.Models;
using QtoRevitPlugin.Services;
using System.Collections.ObjectModel;
using System.Linq;
using System.Threading.Tasks;

namespace QtoRevitPlugin.UI.ViewModels
{
    public partial class ReconciliationViewModel : ObservableObject
    {
        private readonly IQtoRepository _repo;
        private readonly IUserContext _userContext;
        private readonly ModelDiffResult _diffResult;

        [ObservableProperty] private ObservableCollection<DiffEntryViewModel> _deletedItems = new();
        [ObservableProperty] private ObservableCollection<DiffEntryViewModel> _modifiedItems = new();
        [ObservableProperty] private ObservableCollection<Autodesk.Revit.DB.Element> _addedItems = new();

        public int DeletedCount => DeletedItems.Count;
        public int ModifiedCount => ModifiedItems.Count;
        public int AddedCount => AddedItems.Count;

        public ReconciliationViewModel(ModelDiffResult diff, IQtoRepository repo, IUserContext userContext)
        {
            _diffResult = diff;
            _repo = repo;
            _userContext = userContext;

            foreach (var d in diff.Deleted)
                DeletedItems.Add(new DiffEntryViewModel(d));
            foreach (var m in diff.Modified)
                ModifiedItems.Add(new DiffEntryViewModel(m));
            foreach (var a in diff.Added)
                AddedItems.Add(a);
        }

        [RelayCommand]
        private void AcceptAll()
        {
            foreach (var item in DeletedItems.ToList())
                item.AcceptCommand.Execute(null);
            foreach (var item in ModifiedItems.ToList())
                item.AcceptCommand.Execute(null);
        }

        [RelayCommand]
        private void IgnoreAll()
        {
            DeletedItems.Clear();
            ModifiedItems.Clear();
        }
    }

    public partial class DiffEntryViewModel : ObservableObject
    {
        public DiffEntry Entry { get; }
        public string ElementLabel => $"Elem. {Entry.Snapshot.UniqueId[..8]}…";
        public string EpCode => Entry.Snapshot.AssignedEP.FirstOrDefault() ?? "";
        public string Delta => Entry.Delta;
        public string OldQtyLabel => $"{Entry.OldQty:N2}";
        public string NewQtyLabel => $"{Entry.NewQty:N2}";

        [ObservableProperty] private bool _accepted;

        public DiffEntryViewModel(DiffEntry entry) => Entry = entry;

        [RelayCommand]
        private void Accept()
        {
            Accepted = true;
        }
    }
}
```

- [ ] **Step 2: Crea `ReconciliationWindow.xaml`**

```xml
<Window x:Class="QtoRevitPlugin.UI.Views.ReconciliationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Modifiche Modello Rilevate"
        Width="700" Height="550"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="14" FontWeight="Bold" Margin="0,0,0,8"
                   Text="Confronto con lo stato precedente del modello"/>

        <ScrollViewer Grid.Row="1">
            <StackPanel>
                <!-- Eliminati -->
                <TextBlock Text="{Binding DeletedCount, StringFormat='🔴 ELIMINATI ({0})'}"
                           FontWeight="Bold" Foreground="Red" Margin="0,0,0,4"/>
                <ItemsControl ItemsSource="{Binding DeletedItems}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#FFE0E0" BorderThickness="1" Margin="0,2" Padding="6">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding ElementLabel}" Width="130"/>
                                    <TextBlock Text="{Binding EpCode}" Width="100" Foreground="Gray"/>
                                    <TextBlock Text="{Binding OldQtyLabel}" Width="70"/>
                                    <Button Content="Accetta" Command="{Binding AcceptCommand}"
                                            IsEnabled="{Binding Accepted, Converter={StaticResource InverseBool}}"
                                            Margin="8,0,0,0"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Modificati -->
                <TextBlock Text="{Binding ModifiedCount, StringFormat='🟡 MODIFICATI ({0})'}"
                           FontWeight="Bold" Foreground="DarkOrange" Margin="0,8,0,4"/>
                <ItemsControl ItemsSource="{Binding ModifiedItems}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#FFF0D0" BorderThickness="1" Margin="0,2" Padding="6">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding ElementLabel}" Width="130"/>
                                    <TextBlock Text="{Binding OldQtyLabel}" Width="60"/>
                                    <TextBlock Text="→" Width="20"/>
                                    <TextBlock Text="{Binding NewQtyLabel}" Width="60"/>
                                    <TextBlock Text="{Binding Delta}" Width="60" Foreground="DarkOrange"/>
                                    <Button Content="Accetta" Command="{Binding AcceptCommand}"
                                            IsEnabled="{Binding Accepted, Converter={StaticResource InverseBool}}"
                                            Margin="8,0,0,0"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Nuovi -->
                <TextBlock Text="{Binding AddedCount, StringFormat='🔵 NUOVI ({0}) — non ancora computati'}"
                           FontWeight="Bold" Foreground="SteelBlue" Margin="0,8,0,4"/>
                <ItemsControl ItemsSource="{Binding AddedItems}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#D0E8FF" BorderThickness="1" Margin="0,2" Padding="6">
                                <TextBlock Text="{Binding UniqueId}"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="✓ Accetta TUTTO" Command="{Binding AcceptAllCommand}"
                    Background="#1E6FD9" Foreground="White" Padding="12,4" Margin="0,0,8,0"/>
            <Button Content="✗ Ignora TUTTO" Command="{Binding IgnoreAllCommand}"
                    Padding="12,4" Margin="0,0,8,0"/>
            <Button Content="Chiudi" Click="OnCloseClick" Padding="12,4"/>
        </StackPanel>
    </Grid>
</Window>
```

- [ ] **Step 3: Crea `ReconciliationWindow.xaml.cs`**

```csharp
using QtoRevitPlugin.UI.ViewModels;
using System.Windows;

namespace QtoRevitPlugin.UI.Views
{
    public partial class ReconciliationWindow : Window
    {
        public ReconciliationWindow(ReconciliationViewModel vm)
        {
            InitializeComponent();
            DataContext = vm;
        }

        private void OnCloseClick(object sender, RoutedEventArgs e) => Close();
    }
}
```

- [ ] **Step 4: Build Debug R24**

```
msbuild QtoRevitPlugin.sln /p:Configuration="Debug R24" /t:Build /v:m
```

Expected: Build succeeded (XAML converter `InverseBool` può richiedere aggiunta a Resources — se mancante aggiungi `BooleanToVisibilityConverter` o implementa `InverseBoolConverter`)

- [ ] **Step 5: Aggiungi `InverseBoolConverter` se mancante**

Se il build segnala il converter mancante, aggiungi a `QtoTheme.xaml` o al XAML della finestra:

```xml
<Window.Resources>
    <local:InverseBoolConverter x:Key="InverseBool"/>
</Window.Resources>
```

E nel namespace `QtoRevitPlugin.UI`:

```csharp
using System;
using System.Globalization;
using System.Windows.Data;

[ValueConversion(typeof(bool), typeof(bool))]
public class InverseBoolConverter : IValueConverter
{
    public object Convert(object value, Type targetType, object parameter, CultureInfo culture)
        => !(bool)value;
    public object ConvertBack(object value, Type targetType, object parameter, CultureInfo culture)
        => !(bool)value;
}
```

- [ ] **Step 6: Commit**

```bash
git add QtoRevitPlugin/UI/ViewModels/ReconciliationViewModel.cs QtoRevitPlugin/UI/Views/ReconciliationWindow.xaml QtoRevitPlugin/UI/Views/ReconciliationWindow.xaml.cs
git commit -m "feat(sprint8): ReconciliationWindow + ReconciliationViewModel - dialog Model Diff Check"
```

---

### Task 15: Dialog apertura CME — verifica modifiche modello

**Files:**
- Modify: `QtoRevitPlugin/Services/SessionManager.cs` (o dove viene aperta la sessione esistente)

- [ ] **Step 1: Individua il punto di apertura sessione esistente**

Leggi `QtoRevitPlugin/Services/SessionManager.cs` e trova il metodo che apre un file `.cme` esistente (es. `OpenExisting`, `ResumeSession`, o simile).

- [ ] **Step 2: Aggiungi logica di verifica snapshot all'apertura**

Dopo aver caricato la sessione esistente, aggiungi:

```csharp
// Se il file ha snapshot, offri la verifica Model Diff
var snapshots = _repository.GetSnapshots(session.Id);
if (snapshots.Count > 0)
{
    var answer = MessageBox.Show(
        $"Il file contiene {snapshots.Count} elementi con snapshot.\n" +
        "Vuoi verificare le modifiche al modello Revit rispetto all'ultima sessione?",
        "Apri CME esistente",
        MessageBoxButton.YesNo,
        MessageBoxImage.Question);

    if (answer == MessageBoxResult.Yes)
    {
        // Lancia ModelDiff — richiede thread Revit
        LaunchModelDiff(session.Id, snapshots);
    }
}
```

- [ ] **Step 3: Aggiungi `LaunchModelDiff` al SessionManager**

```csharp
private void LaunchModelDiff(int sessionId, IReadOnlyList<ElementSnapshot> snapshots)
{
    _ = Revit.Async.RevitTask.RunAsync(app =>
    {
        var doc = app.ActiveUIDocument?.Document;
        if (doc == null) return;

        var mappingRules = new MappingRulesService();
        var diffSvc = new ModelDiffService(mappingRules);
        var result = diffSvc.ComputeDiff(doc, snapshots, mappingRules);

        if (result.Deleted.Count == 0 && result.Modified.Count == 0 && result.Added.Count == 0)
        {
            // Nessuna modifica
            return;
        }

        // Apri ReconciliationWindow sul thread UI
        System.Windows.Application.Current.Dispatcher.BeginInvoke(() =>
        {
            var vm = new ReconciliationViewModel(result, _repository, _userContext);
            var window = new ReconciliationWindow(vm);
            window.Show();
        });
    });
}
```

- [ ] **Step 4: Build entrambi i target**

```
msbuild QtoRevitPlugin.sln /p:Configuration="Debug R24" /t:Build /v:m
msbuild QtoRevitPlugin.sln /p:Configuration="Debug R25" /t:Build /v:m
```

Expected: entrambi Build succeeded, 0 Error(s)

- [ ] **Step 5: Commit**

```bash
git add QtoRevitPlugin/Services/SessionManager.cs
git commit -m "feat(sprint8): apertura CME esistente - verifica snapshot + lancio ReconciliationWindow"
```

---

### Task 16: Build finale + verifica cross-version

- [ ] **Step 1: Build Release R24 e R25**

```
msbuild QtoRevitPlugin.sln /p:Configuration="Release R24" /t:Build /v:m
msbuild QtoRevitPlugin.sln /p:Configuration="Release R25" /t:Build /v:m
```

Expected: entrambi Build succeeded, 0 Error(s), 0 Warning(s)

- [ ] **Step 2: Run test suite completa**

```
dotnet test QtoRevitPlugin.Tests -v normal
```

Expected: tutti i test PASSED

- [ ] **Step 3: Verifica deploy a Revit 2024**

```
msbuild QtoRevitPlugin.sln /p:Configuration="Debug R24" /t:DeployToRevit2024
```

Expected: file copiati in `%AppData%\Autodesk\Revit\Addins\2024\`

- [ ] **Step 4: Commit finale sprint**

```bash
git add .
git commit -m "chore(sprint6-7-8): build finale verificato su R24+R25 - tutti i test green"
```

---

## Note implementative cross-target

### `ElementId.Value` vs `.IntegerValue`

Ogni uso di `ElementId` come intero deve usare:

```csharp
#if REVIT2024_OR_EARLIER
    var intId = elemId.IntegerValue;
#else
    var intId = (int)elemId.Value;
#endif
```

### `SHA256.HashData`

Disponibile da .NET Framework 4.7.2+ e .NET 5+ — nessuna condizione necessaria.

### `System.Text.Json`

- **net48**: aggiunto via NuGet `System.Text.Json 8.*` (già configurato nel csproj)
- **net8**: built-in

### RevitTask.RunAsync

Tutti i metodi che accedono all'API Revit (doc, selection, parameters) DEVONO girare dentro `await RevitTask.RunAsync(app => { ... })`. Mai accedere all'API Revit direttamente dal thread WPF.
